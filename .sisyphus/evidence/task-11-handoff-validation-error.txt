Task 11 negative-path verification

Negative case covered in `tests/integration/doctor.test.ts`:
- removing `verify_command` from `.rules/patterns/omo-agent-contract.md`

Observed result:
- doctor returns `fail` with `missing handoff schema field verify_command`

Result: PASS
