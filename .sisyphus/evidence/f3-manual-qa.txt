Final Wave F3 manual QA refresh

Scope:
- doctor audit behavior
- install-skill managed workflow expectations
- worktree/bootstrap seam expectations
- landing script behavior

1. Doctor audit behavior

Command exercised:
- `pnpm exec tsx src/cli.ts doctor . --assistant codex --json`

Observed output:
- `"status": "pass"`
- `"passed": 5`
- groups:
  - `codex-runtime: pass`
  - `omo-alignment: pass`
  - `root-scaffold-hints: pass`
  - `deprecated-artifacts: pass`
  - `executables: pass`
- `"missing": []`
- `"invalid": []`
- `"warnings": []`

Interpretation:
- The current repo passes the doctor audit cleanly.
- The surfaced operator path is coherent with the current contract/alignment checks.

2. install-skill managed workflow expectations

Command exercised:
- `pnpm exec tsx src/cli.ts install-skill --assistant opencode --target-root "$tmpdir/opencode-skills" --config-root "$tmpdir/opencode-config" --gsd-root "$tmpdir/.gsd" --json`

Observed output:
- install roots written under a temp directory:
  - `opencode-skills/ai-harness/skills/harness/`
  - `opencode-config/oh-my-opencode.json`
  - `opencode-config/get-shit-done/workflows/autonomous.md`
  - `.gsd/defaults.json`
- reported writes:
  - `writtenPaths`: 6 skill files
  - `writtenConfigPaths`: `oh-my-opencode.json`
  - `writtenWorkflowPaths`: `get-shit-done/workflows/autonomous.md`
  - `writtenDefaultsPaths`: `defaults.json`
- post-install content checks:
  - `opencode-skills/ai-harness/skills/harness/SKILL.md => ok`
  - `opencode-config/oh-my-opencode.json => ok`
  - `opencode-config/get-shit-done/workflows/autonomous.md => ok`
  - `.gsd/defaults.json => ok`

Interpretation:
- `install-skill` refreshes the expected managed OpenCode skill, config, workflow, and GSD defaults surfaces.
- The installed workflow/config content matches current operator-facing expectations.

3. Worktree/bootstrap seam expectations

Current seam file checks:
- `.opencode/worktree.jsonc` contains `"postCreate": ["./.codex/scripts/bootstrap-worktree.sh --quiet"]`
- `.beads/hooks/post-checkout` runs Beads first, then invokes `bootstrap-worktree.sh --quiet`
- `scripts/hooks/post-checkout` keeps the same bootstrap fallback for repos without the plugin or Beads hook

Manual worktree exercise:
- created a temp git repo with copied `.codex/scripts/bootstrap-worktree.sh` and `.beads/hooks/post-checkout`
- added `.env`, `.kamal/secrets`, `.envrc`, and `STICKYNOTE.example.md` in the main worktree
- ran `git worktree add <temp-worktree> -b feat/test-worktree-sync`

Observed output:
- `worktree-setup: linked .env from main worktree`
- `worktree-setup: linked .kamal/secrets from main worktree`
- `worktree-setup: seeded STICKYNOTE.md from STICKYNOTE.example.md`
- `worktree-setup: direnv allow'd .envrc`
- JSON verification:
  - `"envIsSymlink": true`
  - `"kamalIsSymlink": true`
  - `"stickyHasFixture": true`

Interpretation:
- The plugin seam and hook seam point to the same bootstrap path.
- The bootstrap behavior is observable and matches the documented expectations for shared env/secrets and local handoff seeding.

4. Landing script behavior

Manual landing exercise:
- created a temp repo on `main`, attached a bare `origin`, copied `.codex/scripts/land.sh`, and stubbed `pnpm`/`gh`
- added minimal package scripts for `typecheck`, `test`, `test:bdd`, and `test:smoke:dist`
- switched to `feat/land-fixture`
- ran `./.codex/scripts/land.sh`

Observed output:
- branch push succeeded:
  - `* [new branch]      feat/land-fixture -> feat/land-fixture`
  - `branch 'feat/land-fixture' set up to track 'origin/feat/land-fixture'.`
- landing completion:
  - `Landing complete. PR to dev: https://example.test/pr/123`
- quality-gate commands invoked:
  - `typecheck`
  - `test`
  - `test:bdd`
  - `test:smoke:dist`
- GitHub flow invoked:
  - `pr list --head feat/land-fixture --json number,url,baseRefName,state`
  - `pr create --base dev --head feat/land-fixture --fill`
- remote verification:
  - `refs/heads/feat/land-fixture`

Interpretation:
- `land.sh` runs the expected quality gates, pushes the feature branch, and opens or updates a PR to `dev`.
- This manual temp-repo exercise matches the documented execution/autonomous landing path without touching a real remote repository.

5. Cross-check against regression evidence

Command exercised:
- `pnpm test -- tests/integration/install-skill.test.ts tests/integration/bootstrap-worktree.test.ts tests/integration/land-script.test.ts tests/integration/doctor.test.ts`

Observed output:
- `Test Files  4 passed (4)`
- `Tests  23 passed (23)`

Conclusion:
- The current manual QA evidence is consistent with current commands, current files, and current targeted regression coverage.
- The doctor/install-skill/worktree/landing surfaces are coherent and observable in the current repo state.
