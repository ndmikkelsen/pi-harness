# Learnings
- Beads post-checkout hooks can preserve Beads-first ordering and still honor bootstrap fallback by capturing the  exit code, attempting , then returning the original Beads status.
- Beads post-checkout hooks can preserve Beads-first ordering and still honor bootstrap fallback by capturing the bd hook exit code, attempting the bootstrap script, then returning the original Beads status.
- Evidence refreshes must quote the latest command output exactly; targeted regression totals changed during this plan, so final-wave artifacts and the gap report need explicit links to task-level evidence instead of stale summarized counts.
