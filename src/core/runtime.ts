// pi-harness now targets vanilla Pi directly. Provider and model choice live inside Pi runtime setup, not in the scaffold contract.
export const SCAFFOLD_BASELINE = 'pi-native';
export const SCAFFOLD_DISPLAY_NAME = 'Pi-native';