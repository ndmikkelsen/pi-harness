# Dolt database (managed by Dolt, not git)
dolt/
dolt-access.lock

# Runtime files
bd.sock
bd.sock.startlock
sync-state.json
last-touched

# Backup exports (managed by bd, not git)
backup/

# Local version tracking
.local_version

# Worktree redirect file
redirect

# Sync state (local-only, per-machine)
.sync.lock
export-state/

# Ephemeral store
ephemeral.sqlite3
ephemeral.sqlite3-journal
ephemeral.sqlite3-wal
ephemeral.sqlite3-shm

# Dolt server management
dolt-monitor.pid
dolt-server.activity
dolt-server.port
dolt-server.pid
dolt-server.log
dolt-server.lock

# Legacy files
*.db
*.db?*
*.db-journal
*.db-wal
*.db-shm
db.sqlite
bd.db
daemon.lock
daemon.log
daemon-*.log.gz
daemon.pid
